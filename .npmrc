# Use legacy peer deps to resolve React Native conflicts
# This allows npm to bypass peer dependency conflicts for web-only packages
legacy-peer-deps=true

# Additional npm configuration for Vercel builds
engine-strict=false
